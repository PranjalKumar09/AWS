
Amazon Dyanmo DB
    single digit milli second


    NoSQL (key value)

    items as in tables, item represent in json in key pairs
    it offer free tier provide 25 gb of storage, 
    include 25 provisioned write/read capacity units (WCU, RCU) which is enough to handle 200 M request per month


    * paritiion key is primary key

    * it is serverless (means unlike rds, it is straghtforward nothing to put  things  ) 
    * read,write capacity
    * flexible , unstrcutured data

    * automatic sclaling
    * zero downtime
    * on demand pricing
    * idle cost saving (when no request scale down to zero)


    indexes , seconday indexes to fast , 
        backup, 
        point n time recovery (off by default)



    we can use this amazon dyanmo db for our application, we need Region, 

        access key id, secret access id,  (it can get by iam , security credentials through cli)


DB Accelerator
    fully managed in memory cache
    DAX offer microsecond latency achieving up to 10x performance over standard DynamoDB queries
    High availability & scalability
    dax is only used for & is integrated with DynamoDB, while ElasticCache can be used for other databases


DAX is seen in left , bottom side in Dyanmo DB

Global Table is seen in  table , section









AWS Lambda
============
    Like here we just have to upload, its serverless computing that let u run code in response to event without managing servers 


    automatically handle the rest, scalling as need, charge for just function calls

    here we have to deploy for permanent changes & tests for running which can maked private or public 


    It is event driven  service, meaning that it runs your code in response to certian triggers or events


    for event can have trigger
    these event can come from many different aws service like
        s3
        dynamoDB
        api gateway
        cloud watch 

    logs will seen cloud watch
        in log groups section 
    and metrics we can see metrics


    execution time limit: 15 minutes 
    for longer not suitable 
    cold start delays, like after long time , trigger take times

    When to use\
        image processing
            like when perosn upload image in website
        data transformation
        real time notification

    automatically scale
    pay as u go




    and we can upload our code in zip code


    
