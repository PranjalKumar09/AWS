

AWS CloudFront (CDN) that speed up delivery of web content to users by caching is at server (edge location) close to them, improving load times and performance globally

Primary  it cache content like js, image & videos. It can also cache dynamic content 

By default, user data & backend logic is not cached. Cache behavior is controlled via   TTLs, cache behaviors, and origin header



